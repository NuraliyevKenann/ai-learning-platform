# Frontend

React frontend structure for the adaptive learning platform.

The first real screens should support the core loop:

```text
goal selection -> roadmap -> exercise -> feedback -> recommendation
```
